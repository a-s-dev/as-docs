initSidebarItems({"fn":[["define_functions",""],["delete_meta",""],["ensure_url_path","If `p` is a file URL, return it, otherwise try and make it one."],["get_meta",""],["init_sql_connection",""],["normalize_path","As best as possible, convert `p` into an absolute path, resolving all symlinks along the way."],["put_meta",""],["unurl_path","`Path` is basically just a `str` with no validation, and so in practice it could contain a file URL. Rusqlite takes advantage of this a bit, and says `AsRef<Path>` but really means \"anything sqlite can take as an argument\"."]],"struct":[["StorageDb","The entry-point to getting a database connection. No enforcement of this as a singleton is made - that's up to the caller. If you make multiple StorageDbs pointing at the same physical database, you are going to have a bad time. We only support a single writer connection - so that's the only thing we store. It's still a bit overkill, but there's only so many yaks in a day."]]});